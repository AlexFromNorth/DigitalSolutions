# URL бэкенда (Express). Локально не нужен — по умолчанию localhost:3001.
# На Vercel укажи сюда URL развёрнутого API (Railway, Render и т.п.).
# Пример: https://your-api.up.railway.app
NEXT_PUBLIC_API_URL=
